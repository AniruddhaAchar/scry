# .NET build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
artifacts/
publish/

# Rider / VS / VS Code
.vs/
.idea/
*.user
*.suo
*.userprefs

# Test results
[Tt]est[Rr]esult*/
*.trx
*.coverage
coverage/

# NuGet
*.nupkg
.nuget/

# OS
.DS_Store
Thumbs.db

# Local dumps / scratch (never commit memory dumps — they contain secrets)
*.dmp
*.dump
scratch/

# Skill eval workspace: fixture dumps + scratch run outputs/benchmarks. The eval
# *definitions* (skills/scry/evals/evals.json) and the fixture generator
# (samples/fixture-victim) ARE committed; the generated dumps and run artifacts are not.
skills/scry-workspace/

# NOTE: packages.lock.json and global.json ARE committed (intentionally not ignored).
