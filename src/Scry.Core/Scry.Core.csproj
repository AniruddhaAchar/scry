<Project Sdk="Microsoft.NET.Sdk">

  <!-- Scry.Core — shared configuration, logging, and path helpers used by both
       scry (client) and scryd (host). -->
  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Logging" />
  </ItemGroup>

</Project>
